## 1.2.0

- English: Renamed the package to `log_utils_plus` and refreshed the README for publishing.
- 中文：将包名调整为 `log_utils_plus`，并更新 README 以适配发布使用。

## 1.1.0

- English: Added `configure()` for runtime logger customization.
- 中文：新增 `configure()`，支持在运行时自定义日志配置。

- English: Added `resetConfiguration()` and `resetAllTimes()`.
- 中文：新增 `resetConfiguration()` 和 `resetAllTimes()`。

- English: Improved `logTimeEvent()` to return `Duration`.
- 中文：优化 `logTimeEvent()`，使其直接返回 `Duration`。

- English: Added automated tests for log level filtering and timing behavior.
- 中文：补充日志级别过滤和时间打点行为的自动化测试。

- English: Replaced template README and package metadata with usable documentation.
- 中文：将模板化 README 和包元信息替换为可用文档。

## 1.0.1

- English: Initial release.
- 中文：首次发布。
